---
phase: 9
plan: 01
type: diagnostic_test
wave: 1
depends_on:
  - Phase 2
  - Phase 3
  - Phase 4
  - Phase 5
  - Phase 7
files_created:
  - notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb
  - artifacts/diagnostics/dl_pca_test/
  - reports/diagnostics/dl_pca_test/
files_modified: []
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-04
---

<objective>
Create a standalone Colab diagnostic notebook that tests whether the weak DL branch is caused primarily by PCA information loss, architecture mismatch on static vectors, or both, without modifying the main Phase 1-8 source notebooks or existing artifacts.
</objective>

<scope_guard>

## Non-Negotiable Write Boundaries

The executor must not modify:

- `notebooks/01_EDA_Preprocessing.ipynb`
- `notebooks/02_Feature_Engineering.ipynb`
- `notebooks/03_PCA_Feature_Selection.ipynb`
- `notebooks/04_PSO_Model_Training.ipynb`
- `notebooks/05_Hybrid_Ensemble.ipynb`
- `notebooks/06_Adversarial_XAI.ipynb`
- `notebooks/07_Evaluation_Ablation.ipynb`
- `notebooks/08_Final_Report_Kaggle.ipynb`
- `artifacts/models/`
- `artifacts/ensemble/`
- `artifacts/predictions/`
- `reports/tables/phase*.csv`
- `reports/final/`
- `data/final_labeled_fake_reviews.csv`

Allowed writes:

- `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`
- `artifacts/diagnostics/dl_pca_test/**`
- `reports/diagnostics/dl_pca_test/**`
- `.planning/phases/09-dl-pca-diagnostic-test/**`

</scope_guard>

<tasks>

## Task 1: Standalone Diagnostic Notebook Skeleton

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Create a new Colab-only notebook with:

- Colab guard.
- Google Drive mount.
- Project root fallback:
  - `/content/drive/MyDrive/BaoMatCuoiKy/Fake_reviews`
  - `/content/drive/MyDrive/Fake_reviews`
- Read-only input paths:
  - `artifacts/features/features_raw_{train,val,test}.npy`
  - `artifacts/features/labels_{train,val,test}.npy`
  - `artifacts/pca/features_final_{train,val,test}.npy`
  - `artifacts/pca/labels_{train,val,test}.npy`
  - `artifacts/predictions/{dl_pso,xgboost,lightgbm,final_ensemble}_{train,val,test}_prob.npy`
  - `reports/tables/phase7_final_metrics.csv`
  - `reports/tables/phase7_ablation_results.csv`
  - `artifacts/evaluation/phase7_metadata.json`
- Output paths:
  - `artifacts/diagnostics/dl_pca_test/`
  - `reports/diagnostics/dl_pca_test/`

The notebook must state:

- It is a diagnostic test notebook, not a main pipeline phase.
- It does not overwrite any Phase 1-8 artifact.
- It should run on Colab only.

**Acceptance criteria:**

- Notebook exists as a separate file under `notebooks/tests/`.
- No Phase 1-8 notebook is modified.
- Notebook has visible markdown explaining the test question.

## Task 2: Input Validation and Baseline Snapshot

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Implement a validation cell that checks:

- raw feature matrices exist and row counts match labels.
- PCA feature matrices exist and row counts match labels.
- train/val/test labels match between raw feature artifacts and PCA artifacts.
- existing prediction arrays exist for DL PSO, XGBoost, LightGBM and final ensemble.
- expected dimensions:
  - raw fused feature dim: 777.
  - PCA feature dim: 400.

Save:

- `reports/diagnostics/dl_pca_test/input_validation.csv`
- `reports/diagnostics/dl_pca_test/baseline_snapshot.csv`

`baseline_snapshot.csv` should include existing Phase 7 rows:

- DL PSO on PCA.
- LightGBM on PCA.
- final ensemble.
- controlled no-PCA LightGBM from ablation.

**Acceptance criteria:**

- Missing required input fails early with a clear message.
- Baseline snapshot includes the known metrics that motivated the diagnostic test.

## Task 3: Controlled MLP Raw vs PCA Test

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Train two identical lightweight MLP classifiers:

- `mlp_raw_777`: raw fused feature input.
- `mlp_pca_400`: PCA reduced feature input.

Use the same:

- seed 42.
- train/val/test split.
- architecture shape as close as possible, adjusted only for input dimension.
- optimizer.
- max epochs.
- early stopping metric.
- batch size.
- loss function.

Default suggested config:

- `MAX_EPOCHS = 20`
- `PATIENCE = 4`
- `BATCH_SIZE = 128` or lower if OOM.
- `LOSS = BCEWithLogitsLoss` without focal loss/class weighting by default.
- optional class weighting toggle off unless explicitly enabled.

Save:

- `artifacts/diagnostics/dl_pca_test/mlp_raw_test_prob.npy`
- `artifacts/diagnostics/dl_pca_test/mlp_pca_test_prob.npy`
- `reports/diagnostics/dl_pca_test/mlp_raw_vs_pca_metrics.csv`
- `reports/diagnostics/dl_pca_test/mlp_training_history.csv`

**Acceptance criteria:**

- MLP raw and MLP PCA use comparable settings.
- Metrics include Macro F1, Precision Fake, Recall Fake, F1 Fake, ROC-AUC, PR-AUC and runtime.
- The notebook computes `macro_f1_raw_minus_pca`.

## Task 4: Architecture Mismatch Test on PCA Features

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Compare static-vector architectures on the same PCA 400-dim feature input:

- `mlp_pca_400`
- `cnn_bilstm_pca_400_diagnostic`

The CNN-BiLSTM diagnostic model can reuse the conceptual Phase 4 architecture, but must be implemented inside this separate diagnostic notebook and save outputs only to diagnostics folders.

Keep config lightweight:

- reduced hidden sizes if needed.
- no PSO.
- no focal loss by default.
- early stopping.
- same train/val/test split.

Save:

- `artifacts/diagnostics/dl_pca_test/cnn_bilstm_pca_test_prob.npy`
- `reports/diagnostics/dl_pca_test/architecture_mismatch_metrics.csv`
- `reports/diagnostics/dl_pca_test/architecture_training_history.csv`

**Acceptance criteria:**

- The comparison isolates architecture by using the same PCA features.
- The notebook computes `macro_f1_mlp_pca_minus_cnn_bilstm_pca`.
- The interpretation explains whether BiLSTM is likely mismatched with static PCA vectors.

## Task 5: Constrained Non-Zero DL Ensemble Probe

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Run a lightweight probability blend sweep using existing and diagnostic probabilities:

Base probabilities:

- existing `xgboost_test_prob.npy`
- existing `lightgbm_test_prob.npy`
- existing `dl_pso_test_prob.npy`
- diagnostic `mlp_raw_test_prob.npy`
- diagnostic `mlp_pca_test_prob.npy`
- diagnostic `cnn_bilstm_pca_test_prob.npy`

Sweep candidate blends with constraints:

- `dl_source` in `{dl_pso, mlp_raw, mlp_pca, cnn_bilstm_pca}`.
- `dl_weight >= 0.10`.
- separate sweep for `dl_weight >= 0.20`.
- remaining weight split across XGBoost and LightGBM.
- evaluate threshold 0.50 and optional validation-selected threshold.

Save:

- `reports/diagnostics/dl_pca_test/constrained_dl_ensemble_sweep.csv`
- `reports/diagnostics/dl_pca_test/constrained_dl_ensemble_best.csv`

**Acceptance criteria:**

- At least one candidate with non-zero DL is evaluated.
- The report clearly states whether a non-zero DL contribution is viable or materially worse than the unconstrained selected blend.
- The test does not overwrite Phase 5/7 ensemble outputs.

## Task 6: Diagnostic Interpretation and Recommendation

**Type:** implementation  
**Files:** `notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb`

Generate a final diagnostic summary:

- `reports/diagnostics/dl_pca_test/diagnostic_summary.md`
- `reports/diagnostics/dl_pca_test/diagnostic_decision_table.csv`
- `reports/diagnostics/dl_pca_test/runtime_summary.csv`

The decision table must include:

| Question | Evidence | Rule | Decision |
|---|---|---|---|
| Does PCA hurt DL? | MLP raw vs MLP PCA | raw-pca >= 0.02 Macro F1 | yes/no/inconclusive |
| Is CNN-BiLSTM mismatched with PCA vector? | MLP PCA vs CNN-BiLSTM PCA | mlp-cnn >= 0.02 Macro F1 | yes/no/inconclusive |
| Can DL be used in final ensemble? | constrained sweep | non-zero DL within 0.01 Macro F1/ROC-AUC of best | yes/no/inconclusive |
| Should title be kept? | all above | non-zero DL viable and justified | keep/change/retrain |

**Acceptance criteria:**

- Summary gives one of three recommendations:
  1. Keep title and rerun Phase 5/7 with constrained non-zero DL.
  2. Redesign DL branch before keeping title.
  3. Change title/framing to tree ensemble with DL as ablation.
- Summary explicitly references the current issue: existing final model has `dl_weight = 0`.

</tasks>

<verification>

## Plan-Level Verification

Static verification after implementation:

```powershell
python -c "import json, ast; nb=json.load(open('notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb', encoding='utf-8')); [ast.parse(''.join(c.get('source', []))) for c in nb['cells'] if c.get('cell_type')=='code']; print('diagnostic notebook ok')"
```

```powershell
Select-String -Path notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb -Pattern "dl_weight >= 0.10","mlp_raw_777","mlp_pca_400","cnn_bilstm_pca_400","diagnostic_summary.md"
```

```powershell
Select-String -Path notebooks/tests/01_DL_PCA_Diagnostic_Test.ipynb -Pattern "04_PSO_Model_Training.ipynb","05_Hybrid_Ensemble.ipynb","07_Evaluation_Ablation.ipynb","08_Final_Report_Kaggle.ipynb"
```

The last command should show references only as read-only context or scope guards, not write targets.

Colab-run verification after user executes the diagnostic notebook:

- `reports/diagnostics/dl_pca_test/input_validation.csv` exists.
- `mlp_raw_vs_pca_metrics.csv` exists.
- `architecture_mismatch_metrics.csv` exists.
- `constrained_dl_ensemble_best.csv` exists.
- `diagnostic_decision_table.csv` exists.
- `diagnostic_summary.md` exists.

</verification>

<notes>

- This diagnostic test is intentionally narrower than a full retrain.
- The goal is to decide what to do next, not to hide the current DL weight issue.
- If the diagnostic shows non-zero DL is not viable, the project should either redesign DL or change title/framing.

</notes>
