---
phase: 01-data-preparation-and-eda
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - notebooks/01_EDA_Preprocessing.ipynb
autonomous: true
requirements:
  - DATA-01
  - DATA-03
  - DATA-04
user_setup:
  - "Run the completed notebook in Google Colab to generate split files."
must_haves:
  truths:
    - "The notebook saves cleaned data and stratified train/val/test split files under data/processed/."
    - "The split uses a fixed seed and records class distribution for each split."
    - "Phase 2 can read stable split filenames without recomputing Phase 1."
  artifacts:
    - "notebooks/01_EDA_Preprocessing.ipynb includes exports for clean_reviews.csv, train.csv, val.csv, test.csv, and split_metadata.json."
  key_links:
    - "Split export cells consume the cleaned dataframe produced by Plan 02."
---

<objective>
Implement final Phase 1 split/export cells and reproducibility metadata.

Purpose: Create the stable data contract required by Phase 2 feature engineering.
Output: Notebook cells that save cleaned data, stratified split files, and metadata when run in Colab.
</objective>

<execution_context>
@C:/Users/vanhi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vanhi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/NOTEBOOK_PLAN.md
@.planning/phases/01-data-preparation-and-eda/01-CONTEXT.md
@.planning/phases/01-data-preparation-and-eda/01-RESEARCH.md
@.planning/phases/01-data-preparation-and-eda/01-02-PLAN.md
@AGENTS.md
@notebooks/01_EDA_Preprocessing.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cleaned dataset export</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/NOTEBOOK_PLAN.md</read_first>
  <action>
    Add a cell to save the cleaned dataframe as `data/processed/clean_reviews.csv`.
    Also save `data/processed/schema_metadata.json` containing selected text/label/reviewer/product/rating/timestamp columns, original row count, final row count, and cleaning policy settings.
    The code must create parent directories if missing.
  </action>
  <verify>Parse notebook JSON and search for `clean_reviews.csv` and `schema_metadata.json`.</verify>
  <acceptance_criteria>
    - Notebook contains `clean_reviews.csv`.
    - Notebook contains `schema_metadata.json`.
    - Notebook writes to `PROCESSED_DIR`.
  </acceptance_criteria>
  <done>Cleaned data and schema metadata are ready for later phase notebooks.</done>
</task>

<task type="auto">
  <name>Task 2: Add stratified train/val/test split</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/phases/01-data-preparation-and-eda/01-RESEARCH.md</read_first>
  <action>
    Add split code using `sklearn.model_selection.train_test_split` with `random_state=SEED` and `stratify=clean_df[LABEL_COL]`.
    Use default `TEST_SIZE = 0.15` and `VAL_SIZE = 0.15`, computing validation proportion from the non-test remainder.
    Save:
    `data/processed/train.csv`,
    `data/processed/val.csv`,
    `data/processed/test.csv`.
  </action>
  <verify>Parse notebook JSON and search for split filenames and stratify usage.</verify>
  <acceptance_criteria>
    - Notebook contains `train_test_split`.
    - Notebook contains `stratify=`.
    - Notebook contains `train.csv`.
    - Notebook contains `val.csv`.
    - Notebook contains `test.csv`.
  </acceptance_criteria>
  <done>Notebook can produce reproducible stratified split files in Colab.</done>
</task>

<task type="auto">
  <name>Task 3: Add split metadata and Phase 1 completion summary</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
    Add a final notebook section that computes and saves `data/processed/split_metadata.json`.
    Metadata must include seed, split ratios, split row counts, label counts/percentages per split, selected schema columns, output filenames, and timestamp generated in Colab.
    Add a final markdown checklist showing DATA-01 through DATA-04 completion conditions.
  </action>
  <verify>Parse notebook JSON and search for `split_metadata.json` and DATA requirement IDs.</verify>
  <acceptance_criteria>
    - Notebook contains `split_metadata.json`.
    - Notebook contains `DATA-01`.
    - Notebook contains `DATA-02`.
    - Notebook contains `DATA-03`.
    - Notebook contains `DATA-04`.
  </acceptance_criteria>
  <done>Phase 1 has a reproducible handoff contract for Phase 2.</done>
</task>

</tasks>

<threat_model>
Trust boundaries:
- Split files become trusted inputs for all downstream model phases.
- Metadata is the audit trail for reproducibility.

STRIDE register:
- T-DATA-07 Tampering: train/val/test splits are regenerated inconsistently. Mitigation: fixed seed and metadata with row counts/class ratios.
- T-DATA-08 Information disclosure: raw data accidentally copied into reports. Mitigation: data exports stay in `data/processed/`; reports contain aggregate summaries.
- T-DATA-09 Repudiation: unclear split provenance. Mitigation: save split ratios, seed, label distribution, and selected schema columns.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Notebook JSON parses.
- [ ] Notebook contains exports for clean data, train/val/test splits, schema metadata, and split metadata.
- [ ] Split logic uses stratification and fixed seed.
- [ ] No local notebook execution or dataset processing is performed.
- [ ] No `.py` pipeline file is introduced.
</verification>

<success_criteria>

- DATA-01, DATA-03, and DATA-04 are covered.
- Phase 2 can read the split files by stable names.
- Phase 1 output contract is visible inside the notebook.

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-preparation-and-eda/01-03-SUMMARY.md`.
</output>
