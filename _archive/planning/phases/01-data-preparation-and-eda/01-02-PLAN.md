---
phase: 01-data-preparation-and-eda
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - notebooks/01_EDA_Preprocessing.ipynb
autonomous: true
requirements:
  - DATA-02
  - DATA-03
user_setup:
  - "Run this notebook in Google Colab after Plan 01 cells have been implemented."
must_haves:
  truths:
    - "The notebook reports missing values, duplicates, label distribution, review length distribution, and class imbalance."
    - "The notebook applies documented cleaning rules and records before/after row counts."
    - "The cleaning logic does not overwrite the original dataset."
  artifacts:
    - "notebooks/01_EDA_Preprocessing.ipynb exists and includes EDA and cleaning sections."
  key_links:
    - "Cleaning report cells use the schema selected by Plan 01."
---

<objective>
Implement the EDA and cleaning portion of `01_EDA_Preprocessing.ipynb`.

Purpose: Make Phase 1 auditable by showing what was removed or normalized before train/validation/test split.
Output: Notebook sections for full EDA, cleaning policy, and cleaning report generation.
</objective>

<execution_context>
@C:/Users/vanhi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vanhi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/NOTEBOOK_PLAN.md
@.planning/phases/01-data-preparation-and-eda/01-CONTEXT.md
@.planning/phases/01-data-preparation-and-eda/01-RESEARCH.md
@.planning/phases/01-data-preparation-and-eda/01-01-PLAN.md
@AGENTS.md
@notebooks/01_EDA_Preprocessing.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EDA tables and plots</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-data-preparation-and-eda/01-RESEARCH.md</read_first>
  <action>
    Add EDA cells that compute:
    label counts and percentages, text length statistics, missing values per column, duplicate row count, duplicate text/label count, class imbalance ratio, and optional rating/time/reviewer/product summaries when columns exist.
    Add plots for label distribution and review length distribution.
    Configure plot output paths:
    `reports/figures/phase1_label_distribution.png` and `reports/figures/phase1_review_length_distribution.png`.
  </action>
  <verify>Parse notebook JSON and search for expected report filenames.</verify>
  <acceptance_criteria>
    - Notebook contains `phase1_label_distribution.png`.
    - Notebook contains `phase1_review_length_distribution.png`.
    - Notebook computes missing-value counts.
    - Notebook computes duplicate counts.
    - Notebook computes review length.
  </acceptance_criteria>
  <done>EDA covers DATA-02 and produces report-ready tables/figures when run in Colab.</done>
</task>

<task type="auto">
  <name>Task 2: Add transparent cleaning rules</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/phases/01-data-preparation-and-eda/01-CONTEXT.md</read_first>
  <action>
    Add a cleaning section that:
    trims review text, converts empty strings to missing, drops rows missing mandatory text/label, normalizes labels to binary values when mapping is unambiguous, detects duplicates, and drops duplicates using a clearly documented default subset of text plus label.
    Keep configurable flags such as `DROP_DUPLICATES = True` and `DUPLICATE_SUBSET = [TEXT_COL, LABEL_COL]`.
    If label values are unexpected, raise a clear error asking the user to set an explicit mapping.
  </action>
  <verify>Parse notebook JSON and search for cleaning flag and label mapping logic.</verify>
  <acceptance_criteria>
    - Notebook contains `DROP_DUPLICATES`.
    - Notebook contains `DUPLICATE_SUBSET`.
    - Notebook contains label normalization logic.
    - Notebook records row counts before and after cleaning.
  </acceptance_criteria>
  <done>Cleaning behavior is explicit, configurable, and auditable.</done>
</task>

<task type="auto">
  <name>Task 3: Add cleaning report exports</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/NOTEBOOK_PLAN.md</read_first>
  <action>
    Add cells to build and save:
    `reports/tables/phase1_eda_summary.csv` and `reports/tables/phase1_cleaning_report.csv`.
    Include counts for original rows, rows dropped for missing text/label, duplicate rows detected, duplicate rows dropped, final rows, and label distribution after cleaning.
  </action>
  <verify>Parse notebook JSON and search for expected table filenames.</verify>
  <acceptance_criteria>
    - Notebook contains `phase1_eda_summary.csv`.
    - Notebook contains `phase1_cleaning_report.csv`.
    - Notebook writes CSV reports under `REPORT_TABLE_DIR`.
  </acceptance_criteria>
  <done>Phase 1 cleaning decisions are exported for report use.</done>
</task>

</tasks>

<threat_model>
Trust boundaries:
- Raw CSV is an input; cleaned dataframe is generated output.
- EDA figures/tables may later be published.

STRIDE register:
- T-DATA-04 Tampering: cleaning removes too much data silently. Mitigation: record before/after counts and duplicate/missing drops.
- T-DATA-05 Information disclosure: EDA exports raw text by mistake. Mitigation: summary exports should contain aggregate metrics, not full review text.
- T-DATA-06 Denial of service: profiling library consumes too much RAM. Mitigation: use lightweight pandas/seaborn summaries; keep ydata-profiling optional and disabled by default.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Notebook JSON parses.
- [ ] EDA and cleaning sections exist.
- [ ] Report filenames are present.
- [ ] No local dataset execution is performed.
- [ ] No `.py` pipeline file is introduced.
</verification>

<success_criteria>

- DATA-02 is fully covered by EDA logic.
- DATA-03 is covered by explicit cleaning rules and cleaning report export.
- The notebook keeps source data immutable.

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-preparation-and-eda/01-02-SUMMARY.md`.
</output>
