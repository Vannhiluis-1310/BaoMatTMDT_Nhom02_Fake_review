---
phase: 01-data-preparation-and-eda
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/01_EDA_Preprocessing.ipynb
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup:
  - "Run the notebook in Google Colab and mount Google Drive before executing cells."
must_haves:
  truths:
    - "The notebook keeps a Colab-first runtime guard and does not silently run locally."
    - "The notebook defines configurable paths for dataset, processed outputs, report tables, and report figures."
    - "The notebook can infer or explicitly configure mandatory text and label columns."
  artifacts:
    - "notebooks/01_EDA_Preprocessing.ipynb exists and is valid notebook JSON."
  key_links:
    - "DATASET_PATH points to the original CSV, while output paths point to data/processed and reports folders."
---

<objective>
Implement the Phase 1 notebook foundation: Colab guard, path config, imports, reproducibility seed, schema inference, dataset loading cell, and initial EDA helpers.

Purpose: Phase 2 depends on stable cleaned split files, so Phase 1 needs a reliable data-loading and schema-confirmation base.
Output: `01_EDA_Preprocessing.ipynb` with executable Colab cells for setup, schema detection, and first EDA summaries.
</objective>

<execution_context>
@C:/Users/vanhi/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/vanhi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/NOTEBOOK_PLAN.md
@.planning/phases/01-data-preparation-and-eda/01-CONTEXT.md
@.planning/phases/01-data-preparation-and-eda/01-RESEARCH.md
@AGENTS.md
@notebooks/01_EDA_Preprocessing.ipynb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Colab setup and path configuration cells</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>AGENTS.md, .planning/NOTEBOOK_PLAN.md</read_first>
  <action>
    Update the notebook with setup cells that keep the existing local-execution guard, then define imports and constants:
    `SEED = 42`, `PROJECT_ROOT`, `DATASET_PATH`, `PROCESSED_DIR`, `REPORT_TABLE_DIR`, `REPORT_FIGURE_DIR`, `TEST_SIZE = 0.15`, `VAL_SIZE = 0.15`.
    Create the output directories with `Path(...).mkdir(parents=True, exist_ok=True)` inside the notebook code.
    Do not create a separate `.py` module.
  </action>
  <verify>Parse the notebook JSON without executing cells.</verify>
  <acceptance_criteria>
    - Notebook contains `DATASET_PATH`.
    - Notebook contains `PROCESSED_DIR`.
    - Notebook contains `REPORT_TABLE_DIR`.
    - Notebook contains `REPORT_FIGURE_DIR`.
    - Notebook still contains `Run this notebook in Google Colab`.
  </acceptance_criteria>
  <done>The notebook has a complete, Colab-safe configuration section for Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add schema inference and override controls</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/phases/01-data-preparation-and-eda/01-CONTEXT.md</read_first>
  <action>
    Add a cell with explicit override variables:
    `TEXT_COL_OVERRIDE`, `LABEL_COL_OVERRIDE`, `REVIEWER_COL_OVERRIDE`, `PRODUCT_COL_OVERRIDE`, `RATING_COL_OVERRIDE`, `TIMESTAMP_COL_OVERRIDE`.
    Add helper functions to infer column names from candidate lists. Text and label are mandatory; reviewer/product/rating/timestamp are optional.
    Add a clear `ValueError` message instructing the user to set overrides when mandatory columns cannot be inferred.
  </action>
  <verify>Parse notebook JSON and search for override variable names.</verify>
  <acceptance_criteria>
    - Notebook contains `TEXT_COL_OVERRIDE`.
    - Notebook contains `LABEL_COL_OVERRIDE`.
    - Notebook contains `infer_column`.
    - Notebook raises `ValueError` for missing mandatory columns.
  </acceptance_criteria>
  <done>Notebook can support unknown CSV schemas without hard-coding one fragile column layout.</done>
</task>

<task type="auto">
  <name>Task 3: Add initial dataset loading and schema EDA cells</name>
  <files>notebooks/01_EDA_Preprocessing.ipynb</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
    Add notebook cells to load `DATASET_PATH` with pandas, display shape, dtypes, first rows, missing-value counts, duplicate-row counts, inferred schema, and initial label distribution.
    Include a markdown note that the original CSV must not be overwritten.
    Keep code memory-aware: avoid unnecessary dataframe copies and use `low_memory=False`.
  </action>
  <verify>Parse notebook JSON and search for EDA summary strings.</verify>
  <acceptance_criteria>
    - Notebook contains `pd.read_csv`.
    - Notebook contains `low_memory=False`.
    - Notebook contains `df.shape`.
    - Notebook contains `label distribution`.
    - Notebook contains duplicate-row reporting logic.
  </acceptance_criteria>
  <done>Notebook can load the dataset in Colab and show the schema and first EDA checks.</done>
</task>

</tasks>

<threat_model>
Trust boundaries:
- Local workspace contains the original CSV; notebook execution happens in Colab/Drive.
- Generated outputs must not overwrite source data.

STRIDE register:
- T-DATA-01 Tampering: accidental overwrite of original CSV. Mitigation: all writes target `data/processed/` or `reports/`; original path is read-only by convention.
- T-DATA-02 Information disclosure: notebook may display sample review text. Mitigation: limit sample display and avoid publishing raw personally identifying values if present.
- T-DATA-03 Repudiation: unclear schema choices. Mitigation: save schema metadata in later plan and show selected columns in notebook.
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `notebooks/01_EDA_Preprocessing.ipynb` parses as valid JSON.
- [ ] The notebook contains Colab guard, dataset path config, schema override variables, and initial EDA cells.
- [ ] No `.py` pipeline file is introduced.
- [ ] No dataset processing is run locally.
</verification>

<success_criteria>

- DATA-01 is covered by notebook config and load cells.
- DATA-02 is partially covered by initial EDA cells.
- Notebook remains Colab-first.
- The original CSV is preserved.

</success_criteria>

<output>
After completion, create `.planning/phases/01-data-preparation-and-eda/01-01-SUMMARY.md`.
</output>
