# Data Directory

Existing dataset:

```text
data/final_labeled_fake_reviews.csv
```

Use `data/interim/` and `data/processed/` only for generated outputs from the Colab notebook. Do not overwrite the original CSV unless the user explicitly approves it.
